@name Cam Controller
@inputs Cockpit:entity
@outputs 
@persist Chair:entity Driver:entity
@trigger 

interval(100)


Chair = entity():isWeldedTo()


Driver = Chair:driver()
if(Driver:isValid()){
    Driver:setView(1,Cockpit:pos(),Cockpit:angles())
}else{
    reset()
}
