@name 
@inputs 
@outputs 
@persist 
@trigger 

findIncludePlayerProps(owner())
findByClass("sc_weapon_base")
local R = findToArray()

for(I=1,R:count()) {
    local E = R[I,entity]
    E:makeWirelink()
    local W = E:getWirelink()
    W["Fire",number]=1   
}
